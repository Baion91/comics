# Thư viện cho TOOL TẢI TRUYỆN (comic_downloader.py, check_library.py, convert_webp.py).
# Reader (reader_server.py) + supervisor (supervisor.py) chỉ dùng thư viện chuẩn,
# KHÔNG cần cài gì. Server chỉ chạy reader thì có thể bỏ qua file này.
#   python -m venv .venv
#   .venv\Scripts\pip install -r requirements.txt
requests
Pillow
# playwright: CHI can cho site comix.to (Comick). Cai xong PHAI chay them 1 lan
# (cap-nhat.bat tu lam, kem PLAYWRIGHT_BROWSERS_PATH tro vao .reader-meta\pw-browsers):
#   python -m playwright install chromium
# Ghim 1.55: ban 1.62 keo "Chrome for Testing 151" dinh loi side-by-side tren Win10.
playwright==1.55.0
